s3 {
    endpoint = "http://localhost:8003/"
}

sqs {
    endpoint = "http://localhost:8001"
    queue {
        input = "http://localhost:8001/1/testqueueinput"
        output = "http://localhost:8001/1/testqueueoutput"
    }
}
efs {
    root {
        location = "./src/test/resources/testfiles"
    }
}

url {
    auth = "http://localhost:9002/auth"
    api = "http://localhost:9001/graphql"
}

auth {
    client {
        id = "client_id"
        secret = "secret"
    }
}

command = "test.sh"